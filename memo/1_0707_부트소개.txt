스프링 부트의 특징 및 소개
- 부트는 스프링(레거시) 파생된 프로젝트 중 일부이다.
- 개발자가 이용을 많이 하면서, 서브 -> 메인으로 자리 잡음.
- 스프링 -> 부트를 의미함.
- 자동설정.
- 테스트롤 설정 없이 시작 했지만, 보면, 드라이버,
데이터 베이스 관련 설정이 없다고
해서 실행이 안될 뿐이고, 서버가 내장이 되어 있다. (톰캣서버 기본 탑재)
- 배포 할 때, 내장 서버가 포함이 되어서, jar 압축파일로 배포시 편하다.
- xml 설정, root-context, servlet-context. log4j2.xml , 많이 생략이 됨.

- 레거시 마이바티스 프레임워크를 이용했고, 부트에서도 물론이 이용이 가능하고,
- 부트에서는 JPA 이용해서, ORM 기술을 이용해서, 데이터베이스를 처리할 예정.

스프링 부트 프로젝트를 만드는 방법
1) 메이븐(빌드툴)
2) 그래들(빌드툴) 프로젝트
3) 스프링 초기화 프로젝트 생성방법(웹, 인텔리, 이클립스, vscode 지원을함)
많은 곳에서, 쉽게 스프링 부트를 지원하고 있다.


앞으로 설정 관련은 모두 application.properties 파일에서 다 하고,
추가 설정을 @Configuration 어노테이션을 이용해서,
추가 설정하는 경우도 있다.

데이터베이스 -> HikariConfig, 부트에서 자동 탑재

자동 리로딩 설정.

로깅 설정.

데이터베이스 마리아디비 연결.

기존에는 create 테이블을 직접 만들었고,

JPA(영속=1차 캐시 테이블, 임시저장소)
ORM(Object Relation Mapping)
자바 언어 -> 데이터베이스 다루기.





